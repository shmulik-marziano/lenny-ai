# LENNY — מפרט בנייה מלא לכלי עיצוב

## מה לבנות
אפליקציית צ'אט חכמה בשם LENNY לסוכני ביטוח בישראל.
Next.js 14 App Router + TypeScript + Tailwind CSS + Supabase Auth.
RTL מלא (עברית). מובייל-פירסט.

---

## דומיין: lenny-ai.com
## שם המוצר: LENNY
## תת-כותרת: הצ'אט של סוכני הביטוח

---

## מבנה דפים (4 דפים)

### דף 1: דף נחיתה — `/lenny`
דף שיווקי שמוכר את LENNY לסוכני ביטוח.

**Hero Section:**
- כותרת: "LENNY" (גדול, bold)
- תת-כותרת: "הצ'אט שמטפל בכל הבירוקרטיה — אתה רק מאשר"
- תיאור קצר: "תיקון ליקויים, מילוי טפסים, ניתוח תיקים. צ'אט אחד במקום 10 מערכות."
- כפתור CTA ראשי: "התחל בחינם — 5 תיקונים מתנה" → מוביל ל-signup
- כפתור משני: "ראה איך זה עובד" → גולל למטה לדמו

**איך זה עובד (3 שלבים):**
1. 📧 מייל ליקוי נכנס מחברת ביטוח
2. 🔍 לני מזהה את הבעיה ומציע תיקון
3. ✅ אתה מאשר בלחיצה — לני שולח PDF מתוקן

**מספרים (סטריפ אופקי):**
- 13 קטגוריות ליקויים
- 12+ חברות ביטוח
- 989 טפסים ממופים
- 30 שניות לתיקון

**תמחור (3 כרטיסים):**
| מסלול | מחיר | מה כלול |
|-------|------|---------|
| בסיסי | 199₪/חודש | FIX בלבד — תיקון ליקויים |
| מקצועי | 249₪/חודש | FIX + X-RAY (ניתוח תיק) + WISE (המלצות) |
| פרימיום | 299₪/חודש | כל 22 הכלים |

**הערה:** 5 תיקוני ליקויים חינם לכל סוכן חדש.

**Footer:**
- LENNY by SEELD
- קישור לseeld.co.il
- תנאי שימוש | מדיניות פרטיות

---

### דף 2: הרשמה / התחברות — `/lenny/auth`
**עיצוב:** מינימלי, נקי.
- לוגו LENNY למעלה
- שני מצבים: הרשמה | התחברות (טאבים)

**הרשמה:**
- שם מלא
- מספר טלפון
- מייל עבודה
- מספר סוכן (אופציונלי)
- כפתור "התחל בחינם"
- או: התחברות עם Google

**התחברות:**
- מייל
- קוד OTP (נשלח למייל)
- או: התחברות עם Google

**אחרי הרשמה:** ישר לצ'אט.

---

### דף 3: ממשק הצ'אט הראשי — `/lenny/chat`
**זה הלב של המוצר.** ממשק צ'אט כמו WhatsApp/ChatGPT, אבל מותאם לסוכני ביטוח.

**Layout:**
```
┌──────────────────────────────────────────┐
│  [☰]  LENNY          [שם הסוכן] [⚙️]   │  ← Header
├────────────┬─────────────────────────────┤
│            │                             │
│  היסטוריה  │     אזור הצ'אט הראשי       │
│  שיחות     │                             │
│            │  [בועות הודעות]             │
│  • ליקוי   │                             │
│    הראל    │  ┌─────────────────────┐    │
│  • ליקוי   │  │ 🔍 זכוכית מגדלת    │    │
│    פניקס   │  │ תצוגת PDF עם       │    │
│  • ניתוח   │  │ סימון הבעיה        │    │
│    תיק     │  └─────────────────────┘    │
│            │                             │
│            │  [אופציות תיקון]           │
│            │  ☑ עדכן תאריך חתימה        │
│            │  ☑ השלם חתימה עמוד 3       │
│            │  [✅ אשר ושלח]             │
│            │                             │
├────────────┴─────────────────────────────┤
│  [📎] [הקלד הודעה...]        [שלח ➤]   │  ← Input bar
└──────────────────────────────────────────┘
```

**פירוט רכיבי הצ'אט:**

**Header:**
- לוגו LENNY (שמאל/ימין RTL)
- שם הסוכן
- כפתור הגדרות
- מונה שימוש חינמי: "נותרו 3/5 תיקונים חינם"

**סיידבר שמאל (על דסקטופ, drawer על מובייל):**
- רשימת שיחות קודמות
- כל שיחה = מזוהה לפי: שם לקוח + סוג פעולה + תאריך
- כפתור "+ שיחה חדשה"
- חיפוש בשיחות

**אזור צ'אט:**
- בועות הודעות (סוכן = ימין, לני = שמאל)
- הודעות לני כוללות:
  - טקסט רגיל
  - כרטיסי מידע (card) עם פרטי ליקוי
  - תצוגת PDF (זכוכית מגדלת)
  - כפתורי פעולה (אשר/דחה/ערוך)
  - סטטוס שליחה (נשלח ✓, התקבל ✓✓)

**רכיב "זכוכית מגדלת" (הרכיב המרכזי):**
```
┌─────────────────────────────────────┐
│  📋 ליקוי: עדכון תאריכי חתימה      │
│  👤 לקוח: משה כהן (ת.ז 12345678)  │
│  🏢 חברה: הראל                      │
│  📑 קטגוריה: הצטרפות / קליטה       │
│                                     │
│  ┌───────────────────────────┐      │
│  │                           │      │
│  │   [תמונת PDF עם סימון    │      │
│  │    אדום/כתום על המקום     │      │
│  │    שדורש תיקון]           │      │
│  │                           │      │
│  │   🔍 זום: + / -           │      │
│  └───────────────────────────┘      │
│                                     │
│  תיקון מוצע:                        │
│  ☑ עדכון תאריך מ-01/01/2025         │
│    ל-25/03/2026 (13 מופעים)         │
│  ☑ השלמת חתימה בעמוד 7              │
│                                     │
│  שולחים חזרה ל:                     │
│  hitspension@harel-ins.co.il        │
│                                     │
│  [✅ אשר ושלח]  [✏️ ערוך]  [❌]    │
└─────────────────────────────────────┘
```

**Input Bar:**
- שדה טקסט (placeholder: "שלח מייל ליקוי, שאל שאלה, או בקש פעולה...")
- כפתור צירוף קובץ (PDF, תמונה, MSG)
- כפתור שליחה
- drag & drop לקבצים

**סוגי הודעות של לני:**

1. **הודעת ברכה (פעם ראשונה):**
   "היי [שם], אני לני 👋 העוזר שלך לכל מה שקשור לביטוח. תשלח לי מייל ליקוי ואני אטפל."

2. **זיהוי ליקוי:**
   "זיהיתי ליקוי מהראל — עדכון תאריכי חתימה עבור משה כהן. רגע מכין..."

3. **כרטיס תיקון (זכוכית מגדלת):**
   [הרכיב שלמעלה]

4. **אישור שליחה:**
   "✅ PDF מתוקן נשלח ל-hitspension@harel-ins.co.il. העתק נשמר."

5. **בקשת מידע:**
   "חסר לי תאריך חתימה חדש. מה התאריך הנכון?"

---

### דף 4: הגדרות — `/lenny/settings`
- פרטי סוכן (שם, מייל, טלפון, מספרי סוכן)
- חיבור מיילים (IMAP/Gmail API)
- העדפות התראות
- מנוי ותשלום
- היסטוריית שימוש

---

## עיצוב ומיתוג

### פלטת צבעים (הצעה — עדכן לפי המיתוג שלך):
- **רקע ראשי:** #0A0F1E (כהה כמעט שחור)
- **רקע צ'אט:** #111827 (אפור כהה)
- **צבע ראשי (accent):** #00B4D8 (ציאן/תכלת)
- **צבע משני:** #F4A261 (זהב/כתום)
- **הצלחה:** #22C55E (ירוק)
- **שגיאה:** #EF4444 (אדום)
- **טקסט:** #F9FAFB (לבן-כמעט)
- **טקסט משני:** #9CA3AF (אפור)
- **בועה סוכן:** #1E3A5F
- **בועה לני:** #1F2937

### פונטים:
- כותרות: **Heebo** (Bold/Black)
- גוף: **Heebo** (Regular)
- קוד/מספרים: **JetBrains Mono** או **Fira Code**

### אייקונים:
- Lucide React או Heroicons
- סגנון: outline, 24px

### עקרונות עיצוב:
- **מובייל-פירסט** — 80% מהסוכנים בטלפון
- **RTL מלא** — dir="rtl", כל הטקסט מימין לשמאל
- **דארק מוד כברירת מחדל** — סוכנים עובדים שעות ארוכות
- **מינימלי** — אין דשבורדים, אין גרפים, רק צ'אט
- **מהיר** — תחושה של WhatsApp, לא של ERP

---

## טכנולוגיה

### Stack:
- **Framework:** Next.js 14 App Router
- **Language:** TypeScript
- **Styling:** Tailwind CSS
- **UI Components:** shadcn/ui (או Radix UI)
- **Auth:** Supabase Auth (Google + OTP)
- **Database:** Supabase (PostgreSQL)
- **AI:** Claude API (Anthropic) — לזיהוי ליקויים
- **Storage:** Supabase Storage (PDFs)
- **Deployment:** Vercel
- **Domain:** lenny-ai.com

### טבלאות DB (Supabase):
```sql
-- סוכנים
CREATE TABLE agents (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT UNIQUE NOT NULL,
  full_name TEXT NOT NULL,
  phone TEXT,
  agent_numbers JSONB DEFAULT '{}',
  free_fixes_remaining INT DEFAULT 5,
  plan TEXT DEFAULT 'free',
  created_at TIMESTAMPTZ DEFAULT now()
);

-- שיחות
CREATE TABLE conversations (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  agent_id UUID REFERENCES agents(id),
  title TEXT,
  customer_name TEXT,
  customer_id_number TEXT,
  company TEXT,
  category TEXT,
  status TEXT DEFAULT 'active',
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

-- הודעות
CREATE TABLE messages (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  conversation_id UUID REFERENCES conversations(id),
  role TEXT NOT NULL CHECK (role IN ('agent', 'lenny', 'system')),
  content TEXT NOT NULL,
  message_type TEXT DEFAULT 'text',
  metadata JSONB DEFAULT '{}',
  created_at TIMESTAMPTZ DEFAULT now()
);

-- ליקויים
CREATE TABLE deficiencies (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  conversation_id UUID REFERENCES conversations(id),
  agent_id UUID REFERENCES agents(id),
  customer_name TEXT NOT NULL,
  customer_id_number TEXT,
  company TEXT NOT NULL,
  category TEXT NOT NULL,
  source_email TEXT,
  return_email TEXT,
  original_pdf_url TEXT,
  fixed_pdf_url TEXT,
  fix_description TEXT,
  status TEXT DEFAULT 'pending',
  created_at TIMESTAMPTZ DEFAULT now(),
  fixed_at TIMESTAMPTZ
);
```

### API Routes:
```
POST /api/chat          — שליחת הודעה + קבלת תגובת לני
POST /api/upload        — העלאת PDF/MSG
POST /api/fix/analyze   — ניתוח ליקוי ב-PDF
POST /api/fix/apply     — ביצוע תיקון
POST /api/fix/send      — שליחת PDF מתוקן במייל
GET  /api/conversations — רשימת שיחות
GET  /api/stats         — סטטיסטיקות שימוש
```

---

## Flow ראשי — תיקון ליקוי

```
סוכן מעלה קובץ PDF/MSG לצ'אט
        ↓
לני: "מנתח את הקובץ..."
        ↓
לני מזהה:
  - חברה: הראל (לפי כתובת מייל השולח)
  - לקוח: משה כהן (לפי OCR)
  - קטגוריה: הצטרפות (לפי סוג הליקוי)
        ↓
לני מציג "זכוכית מגדלת":
  - תמונת PDF עם סימון על הבעיה
  - תיקון מוצע
  - כתובת מייל להחזרה
        ↓
סוכן לוחץ "אשר ושלח"
        ↓
לני:
  1. מתקן את ה-PDF
  2. שולח למייל חברת הביטוח
  3. שומר העתק
  4. מעדכן: "✅ נשלח בהצלחה"
```

---

## הערות חשובות לכלי העיצוב

1. **כל הממשק בעברית** — כפתורים, הודעות, placeholder-ים
2. **RTL** — dir="rtl" על html ועל כל הקומפוננטות
3. **פונט Heebo** — מ-Google Fonts
4. **הצ'אט הוא המוצר** — לא לבנות דשבורד, לא CRM, רק צ'אט
5. **רספונסיבי** — עובד מושלם על מובייל (375px) ודסקטופ (1440px)
6. **הסיידבר נסגר על מובייל** — hamburger menu
7. **הודעות עם אנימציה** — fade in כמו בצ'אט אמיתי
8. **כפתורי פעולה בתוך הצ'אט** — לא בדף נפרד, הכל בזרימת השיחה
9. **drag & drop** — גרירת קבצים לאזור הצ'אט
10. **סקלטון לודינג** — כשלני "חושב", מראה typing indicator

---

## מה לא לבנות (עוד)
- ❌ דשבורד עם גרפים
- ❌ טבלאות לקוחות
- ❌ מערכת הרשאות מורכבת
- ❌ אינטגרציה עם WhatsApp (שלב מאוחר)
- ❌ תשלומים (שלב מאוחר)
- ❌ ממשק אדמין

רק: דף נחיתה + auth + צ'אט + הגדרות בסיסיות.
